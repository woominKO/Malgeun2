<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style3.css">
    <title><%=title %></title>
    
</head>
<body>
    <div class="content-container">
        <div class="text-holder">
        <h1><%= title %></h1>
        <p><%= detail %></p>
        </div>
        <div class="img-holder">
            <% if (image && image !== '') { %>
                <img src="<%= image %>" alt="첨부 이미지" style="max-width: 100%; height: auto;">
            <% } else { %>
                <p>첨부된 이미지가 없습니다.</p>
            <% } %>
        </div>
        <a href="/notice">목록으로 돌아가기</a>
    </div>
    
</body>
</html>
